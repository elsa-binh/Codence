# Codence

This repository contains a simple React + Material UI frontend scaffolded with Vite.

## Getting started

1. Open a terminal in `d:\Work\Codence\Codence`
2. Run `npm install`
3. Run `npm run dev`
4. Open the local URL shown in the terminal
